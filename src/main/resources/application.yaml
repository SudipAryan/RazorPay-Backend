spring:
  application:
    name: razorpay

  datasource:
    url: jdbc:postgresql://localhost:5432/RazorPayDB
    username: postgres
    password: Sudip@22

  jpa:
    hibernate:
      ddl-auto: create

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect